server:
  port: 8082

spring:
  application:
    name: AMAZON-cart-service

  datasource:
    url: jdbc:mysql://localhost:3306/amazon_cart_db
    username: root
    password: Anijjk9708@#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka