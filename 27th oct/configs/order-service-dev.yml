spring:
  application:
    name: order-service

  datasource:
    url: jdbc:mysql://localhost:3306/amazon   # changed from mysql → localhost
    username: root
    password: Test@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
